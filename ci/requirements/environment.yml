name: xweights-tests
channels:
    - conda-forge
    - nodefaults
dependencies:
    - dask
    - geopandas
    - xarray
    - cartopy
    - xesmf
    - numpy
    - setuptools
    - pooch
    - netCDF4
    # for testing
    - pytest
    - pytest-cov
    - pytest-env
    - pip

    - pip:
      - pyhomogenize
